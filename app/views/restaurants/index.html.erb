<h1>Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li> <%= link_to restaurant.name, restaurant_path(restaurant) %> | <%= link_to "x", restaurant_path(restaurant), method: :delete, data: { confirm: "are you sure?"} %> <%= link_to "edit restaurant", edit_restaurant_path(restaurant) %> </li>

  <% end %>
</ul>

<%= link_to "add a new restaurant", new_restaurant_path %>
